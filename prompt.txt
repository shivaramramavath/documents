You are an expert Node.js backend engineer, software architect, JavaScript/TypeScript expert, technical educator, and professional documentation writer.

I am building a comprehensive Node.js Basic → Advanced → Production mastery and reference repository.

I will provide ONLY a Markdown filename/topic, for example:

10-routing.md

Your task is to generate the COMPLETE contents of that exact Markdown file.

Treat the provided filename/topic as the authoritative scope of the document.

IMPORTANT RULES:

Do not ask me what to include.
Do not provide an outline instead of the actual documentation.
Do not provide a short tutorial.
Do not skip beginner, intermediate, advanced, internal, or production-level concepts that genuinely belong to the topic.
Do not skip important, rarely used, legacy, advanced, or edge-case concepts when they are relevant.
Do not add unrelated concepts simply to make the document longer.
Do not leave placeholders.
Do not write "add more here".
Do not write "and so on".
Do not hide missing information behind "etc.".
Do not tell me to search elsewhere.
Do not ask follow-up questions.
Do not create additional files.
Do not mention this prompt.
Do not mention that you are an AI.
Do not provide explanations outside the Markdown document.
Do not repeat the filename outside the document.
The output must be directly usable as the contents of the requested .md file.
Return ONLY the complete Markdown file content.
Do not wrap the entire response in a Markdown code fence.

The document must work as both:

A learning guide for studying the topic from beginner to advanced.
A permanent technical reference for revisiting the topic years later.

DOCUMENTATION LEVEL:

Write like professional Node.js documentation combined with an expert engineering reference.

Use precise technical terminology.

Explain both WHAT something is and WHY it works that way.

Prefer practical, technically correct examples.

Clearly distinguish between:

JavaScript language features
Node.js runtime behavior
Node.js built-in APIs
Browser APIs
Third-party packages
Framework APIs
Operating-system behavior
HTTP behavior
Database behavior

Never present a third-party library API as a Node.js core API.

Use modern Node.js practices by default while documenting important legacy/deprecated APIs when they are relevant to real-world maintenance.

DOCUMENT STRUCTURE:

1. Title

Create a clear H1 title based on the requested topic.

2. Overview

Explain:

What the topic is
Why it exists
What problem it solves
Why it matters
Where it is used in Node.js
When it should be used
When it should not be used
How it fits into the Node.js ecosystem
Common real-world use cases
Relationship with closely related concepts
3. Prerequisites

List:

Required

Concepts that should already be understood.

Recommended

Concepts useful for understanding advanced sections.

Only include genuinely relevant prerequisites.

4. Mental Model

Explain the concept internally and conceptually.

Use Markdown/ASCII diagrams when useful.

For example:

Client
|
v
Request
|
v
Node.js Server
|
v
Router
|
+---- Route A ----> Handler A
|
+---- Route B ----> Handler B
|
v
Response
|
v
Client

Explain:

Components
Relationships
Data flow
Control flow
Lifecycle
Execution order
Internal interactions

Do not force diagrams where they are not useful.

5. Core Concepts

Explain every fundamental concept belonging to the requested topic.

For every significant concept, cover when applicable:

Definition
Purpose
Why it exists
Problem it solves
Syntax
API
Parameters
Return value
Behavior
Lifecycle
Example
Common mistakes
Important notes
Production considerations